import:
    - /$HOME/.config/alacritty/dracula.yml

font:
    normal: 
        family: JetBrainsMono Nerd Font Mono
        style: Regular

    size: 15     

shell:
  program: /bin/zsh
  args:
    - -l
    - -c
    - "tmux attach -u || tmux -u"
